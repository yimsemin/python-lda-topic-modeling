# AI 작업 지침

이 저장소는 공개된 프로젝트이므로 기존 사용자의 코드와 결과 재사용을 깨지 않는 것을 우선한다.

## 작업 시작

- 먼저 `git status --short`와 관련 문서·코드를 확인한다.
- 기존 변경과 겹치면 내용을 보존하고, 충돌 가능성이 있으면 수정 전에 사용자에게 확인한다. 관련 없는 파일은 함께 수정하지 않는다.
- `input/`과 `output/`은 사용자 데이터 영역이다. 명시적 요청 없이 삭제하거나 덮어쓰지 않고, 검증에는 `test/input/`과 `test/output/`을 우선 사용한다.

## 호환성

- 기존 공개 진입점은 유지한다. 여기에는 파일명, 함수명, 설정 함수명, 인자, 반환값, 실행 방식이 포함된다.
- 결과 파일의 이름, 저장 위치, CSV 컬럼명처럼 사용자가 후처리에 의존할 수 있는 출력 형식도 유지한다.
- 위 호환성을 깨야만 버그를 고칠 수 있거나 기능을 추가할 수 있다면 구현 전에 사용자 승인을 받는다.

## 변경 방식

- 기존 구조와 흐름을 최대한 유지하면서 필요한 부분만 작게 수정한다.
- 새 파일, 새 함수, 새 클래스, 새 의존성은 기존 코드 안에서 해결하기 어렵거나 중복을 분명히 줄일 때만 추가한다.
- 기능과 직접 관련 없는 포맷 변경, 파일 이동, 스타일 정리는 피한다.

## 파일 형식

- 코드와 문서는 LF 줄 끝을 사용한다. CRLF로 저장하거나 기존 파일의 줄 끝을 불필요하게 바꾸지 않는다.
- 파일 수정 후에는 가능한 경우 `git diff --check`로 whitespace error와 CRLF 관련 문제를 확인한다.
- 인코딩은 기존 파일과 동일하게 유지하고, 특별한 이유가 없으면 UTF-8 텍스트로 저장한다.

## 실행 환경

- 파이썬 실행과 검증은 Windows에서 `.venv\Scripts\python.exe`, macOS·Linux에서 `.venv/bin/python`을 사용한다.
- 시스템 `python`, `python3`, Codex 번들 런타임은 `.venv`를 사용할 수 없을 때만 쓰고, 그 이유를 완료 보고에 적는다.

## 문서

- `README.md`를 현재 구현된 공개 기능과 사용법의 기준 문서로 유지한다.
- `doc/PLAN_*.md`는 미구현 계획, `doc/TEST_DATA_*.md`는 샘플 데이터의 생성 근거와 세부 정보로 구분한다. 계획을 현재 기능으로 간주하거나 별도 요청 없이 구현하지 않는다.
- AI 작업 원칙과 유지보수 규칙은 이 파일에만 적는다.
- 같은 내용을 여러 문서에 반복하지 않고 기준 문서로 연결한다.

## 검증

- 별도 지시가 없으면 변경 범위에 맞춘 최소 검증만 수행한다.
- 긴 전체 파이프라인 실행은 사용자가 직접 확인할 수 있도록 남기고, AI는 문법 확인이나 좁은 범위의 스모크 테스트를 우선한다.
- 실행하지 못했거나 확인하지 못한 부분은 완료 보고에 적는다.

## 주석

- 사용자가 실수하기 쉬운 설정값, 입력 데이터 형식, 결과 해석, 예외 처리에는 짧은 주석을 둔다.
- 코드만 봐도 알 수 있는 설명형 주석은 추가하지 않는다.
- 기존 주석의 언어와 톤을 따른다.
